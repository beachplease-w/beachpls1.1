<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="icon" href="<%= image_path('tab-icon') %>">
    <title>BeachPlease</title>
    <meta name="description" content="Changez votre façon de réserver vos tournois de Beach Volley !">
    <link href="https://fonts.googleapis.com/css?family=Nixie+One|Quicksand:400,700" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= stylesheet_pack_tag 'application', media: 'all' %>
  <body>
    <div class="wrapper">
      <%= render 'shared/navbar' %>
      <%= render 'shared/flashes' %>




      <!-- <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p> -->

      <%= yield %>
      <%= render 'shared/footer' %>
    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
    </div>
  </body>
</html>
